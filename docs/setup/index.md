# Setup

Get your development environment running with a single command.

`setup.sh` detects your OS and installs packages, shell config, fonts, terminal settings, and AI coding tools.

```bash
./setup.sh
```

See the subpages for platform details, section reference, and selective run options.
