# CLAUDE.md

Repository instructions live in `AGENTS.md`, which every agent tool in use here
reads. This file exists only because Claude Code does not discover `AGENTS.md`
on its own, so it imports it.

@AGENTS.md
